import React from 'react';

const SwapButton = () => {
    return (
        <button
            className="px-16 py-3.5 mt-12 text-center text-white whitespace-nowrap bg-blue-800 rounded-[32px] max-md:px-5 max-md:mt-10 max-md:max-w-full"
            aria-label="Swap"
        >
            Swap
        </button>
    );
};

export default SwapButton;